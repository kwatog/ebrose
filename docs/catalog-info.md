# Backstage catalog info

This repo includes Backstage catalog metadata and TechDocs configuration:

- `catalog-info.yaml` — Backstage entity definition (Component).
- `mkdocs.yml` + `docs/*` — TechDocs source.

To register in Backstage, point the catalog to `catalog-info.yaml`.

